import 'package:flutter/material.dart';

class GetSponsor extends StatefulWidget {
  const GetSponsor({Key key}) : super(key: key);

  @override
  _GetSponsorState createState() => _GetSponsorState();
}

class _GetSponsorState extends State<GetSponsor> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Card(
          child: Text("Coming soon..."),
        ),
      ),
    );
  }
}
